#include<stdio.h>
main()
{
int n,r,s=0,r1;
	printf("enter number\n");
	scanf("%d",&n);
	r1=n;
	while(n!=0)
	{
		r=n%10;
		s=s*10+r;
		n=n/10;
	}
	
	printf("reverse number: %d\n",s);
	
	if(r1==s)
	printf("palindrome");
	else
	printf("not a palindrome");

}
